{% extends 'base.html.twig' %}


{% block body %}

    <div style="margin-top: 5%" class="container-fluid bg-3 text-center">
        <div class="row">
            <div class="col-sm-6">
                {% if news != null %}
                    {% for News in news %}
                    <div class="col-sm-12">
                        <p><span class="flotte"><img src="/img/news/{{News.file}}"></span>
                            <a href="{{ path('NewsDetail',{'id': News.id}) }}">{{ News.titre }}</a>
                        </p>
                    </div>
                    {% endfor %}
                {% else %}
                    <p>Aucune news actuellement</p>
                {% endif %}

            </div>



            <div class="col-sm-6">

                   {% for Concours in concour %}
                       {% if app.user %}
                       <div class="col-sm-12">
                            <figure>
                                <a href="{{ path('InscriptionConcours',{'id':Concours.id}) }}"><img src="../img/concours/{{Concours.file}}"></a>
                            </figure>
                       </div>
                           {% else %}
                               <div class="col-sm-12">
                                   <figure>
                                       <a href="{{ path('Login')}}">Participez à nos concours</a>
                                   </figure>
                               </div>
                    {% endif %}

                    {% endfor %}
            </div>
        </div>
    </div>

{% endblock %}



